use Mix.Config

config :ghdump,
  # GitHub access token for interacting with the API
  #access_token: "github-access-token",

  # GitHub organisation name from which repo's user's contributions will be diffed
  #org_name: "your-org",

  # Ftp server host where the data shall be delivered
  #host: '127.0.0.1',

  # Ftp server username
  #username: 'ftp-username',

  # Ftp server password
  #password: 'ftp-password',

  # Directory to which the files shall be delivered
  #target_dir: 'remote/dir',

  # Local dir for cached dumps
  #filedir: "/tmp/local/dir"
