# Ghdump

An example app for grabbing data from GitHub with Flow. 

## Installation

To install use mix:

```bash
mix deps.get
mix escript.build
mix escript.install
```

## Configuration

The app is configred in [config files](/config).
